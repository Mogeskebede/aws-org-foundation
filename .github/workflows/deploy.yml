name: Destroy AWS Org Foundation

on:
  workflow_dispatch:


permissions:
  id-token: write
  contents: read

jobs:
  destroy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Install CDK
      run: npm install -g aws-cdk

    - name: Destroy stacks
      run: cdk destroy --all --force

# name: Deploy AWS Org Foundation

# on:
#   push:
#     branches:
#     - main

# permissions:
#   id-token: write
#   contents: read

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     env:
#       CDK_DEPLOY_REGION: ${{ secrets.AWS_REGION }}
#       CDK_DEPLOY_ACCOUNT: ""
#       ORG_PARENT_OU_ID: ${{ secrets.ORG_PARENT_OU_ID }}
#       SSO_INSTANCE_ARN: ${{ secrets.SSO_INSTANCE_ARN }}

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Configure AWS credentials (OIDC)
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
#         aws-region: ${{ secrets.AWS_REGION }}

#     - name: Export AWS account ID to env
#       id: aws-account
#       run: |
#         ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
#         echo "CDK_DEPLOY_ACCOUNT=$ACCOUNT_ID" >> $GITHUB_ENV

#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: "3.11"

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#         npm install -g aws-cdk

#     - name: CDK bootstrap (idempotent)
#       run: |
#         cdk bootstrap aws://$CDK_DEPLOY_ACCOUNT/${{ secrets.AWS_REGION }}

#     - name: CDK synth
#       run: |
#         cdk synth

#     - name: CDK deploy
#       run: |
#         cdk deploy --all --require-approval never
